# This workflow is intentionally minimal.
# Notification for AGENT_CHANGELOG.md changes is handled via GitHub Watch emails.
#
# To activate: go to your pm-agents repo on GitHub, click Watch (top right),
# select All Activity. GitHub will email you at your account address
# whenever any file - including AGENT_CHANGELOG.md - is committed.
#
# This file is kept in the repo so the .github/workflows/ folder exists
# and can be extended later (e.g. to add CI checks or linting on agent files).

name: Agent System - Reserved for Future Use

on:
  workflow_dispatch:  # Manual trigger only - no automatic runs

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "No automated steps configured. Notifications handled via GitHub Watch."
